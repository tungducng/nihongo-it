name: Backend CI

on:
  push:
    branches: [main]
    paths:
      - 'services/**'
      - '.github/workflows/backend.yml'
  pull_request:
    branches: [main]
    paths:
      - 'services/**'
      - '.github/workflows/backend.yml'

permissions:
  contents: read
  # Needed so madrapps/jacoco-report can post a coverage comment to the PR.
  pull-requests: write

jobs:
  build-and-test:
    name: Build & Test (${{ matrix.service }})
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        service:
          - user-service
          - learning-service
          - notification-service
          - ai-service
          - api-gateway

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'zulu'

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('services/**/*.gradle.kts') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission
        run: chmod +x services/gradlew

      - name: Build common module
        run: ./gradlew :common:build -x test
        working-directory: services

      - name: Build service
        run: ./gradlew :${{ matrix.service }}:build -x test
        working-directory: services

      - name: Run tests + JaCoCo verification
        # `check` includes jacocoTestCoverageVerification on user/learning;
        # other services have no coverage gate yet but still run tests.
        run: ./gradlew :${{ matrix.service }}:check
        working-directory: services

      - name: Upload JaCoCo report
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: jacoco-${{ matrix.service }}
          path: services/${{ matrix.service }}/build/reports/jacoco/
          retention-days: 7

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v5
        with:
          name: test-report-${{ matrix.service }}
          path: services/${{ matrix.service }}/build/reports/tests/
          retention-days: 7

      - name: Post coverage comment on PR
        if: github.event_name == 'pull_request' && always()
        uses: madrapps/jacoco-report@v1.7.2
        continue-on-error: true
        with:
          paths: services/${{ matrix.service }}/build/reports/jacoco/test/jacocoTestReport.xml
          token: ${{ secrets.GITHUB_TOKEN }}
          min-coverage-overall: 15
          min-coverage-changed-files: 30
          title: 'Coverage — ${{ matrix.service }}'
          update-comment: true
